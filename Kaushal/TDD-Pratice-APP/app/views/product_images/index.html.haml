.row
  .col-md-6 
    %h1 
      = "Listing #{@product.blank? ? 'All' : @product.name} Template"
  .col-md-6
    .pull-right.invite_btn
      - if policy(ProductImage).create?
        = link_to 'New Template', new_product_product_image_path(@product), class: 'btn btn-default'
= render 'list'